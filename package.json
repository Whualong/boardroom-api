{
  "name": "open-im",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm run build:web && pnpm run build:api",
    "dev:web": "pnpm -F @openim/web dev",
    "dev:api": "pnpm -F @openim/api start:dev",
    "build:web": "pnpm -F @openim/web build",
    "build:api": "pnpm -F @openim/api build"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}